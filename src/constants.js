// The number of pixels, from the center of the screen in either direction, where all of the hour boxes are fully visible
export const middleBoxViewX = 300;

// The number of pixels, starting from the middleBoxViewX and moving out in either direction,
// where the hour boxes start to fade
export const middleBoxFadeWidth = 120;
